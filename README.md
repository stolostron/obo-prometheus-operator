# obo-prometheus-operator

Builds the obo-prometheus-operator used in COO. The code is imported using a submodule wich the Containerfile uses as a source for the build.

To update to a new version of the obo-prometheus-operator, simply bump the submodule reference.
